import { SppSidebarPortalDirective } from './spp-sidebar-portal.directive';

describe('SidebarPortalDirective', () => {
  it('should create an instance', () => {
    const directive = new SppSidebarPortalDirective();
    expect(directive).toBeTruthy();
  });
});
